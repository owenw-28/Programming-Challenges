def main():
    keys = ['a', 'b', 'c', 'd']
    values = [1, 2, 3]
    dictionary = createDict(keys, values)

def createDict(keys, values):
    add_dict = []
    for i in keys, values:
        add_dicts = [keys:values]
    return add_dicts
        
